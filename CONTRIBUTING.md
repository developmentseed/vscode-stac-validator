# Contributing

To run tests:

```sh
yarn install
yarn test
```

We don't run tests in CI because @gadomski hasn't figured out how to get the display server working correctly, and this is a hobby project.
