{
  "name": "vscode-stac-validator",
  "displayName": "STAC Validator",
  "description": "Automatically validate SpatioTemporal Asset Catalog (STAC) JSON files",
  "author": {
    "name": "Pete Gadomski",
    "email": "pete.gadomski@gmail.com"
  },
  "version": "0.0.4",
  "publisher": "developmentseed",
  "repository": {
    "url": "https://github.com/developmentseed/vscode-stac-json-schema"
  },
  "license": "MIT",
  "icon": "img/STAC-04.png",
  "engines": {
    "vscode": "^1.106.1"
  },
  "categories": [
    "Linters"
  ],
  "keywords": [
    "stac",
    "json-schema",
    "validation",
    "geospatial"
  ],
  "activationEvents": [
    "onLanguage:json"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "STAC JSON Schema Validator",
      "properties": {
        "stacJsonSchema.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic STAC JSON schema validation"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "yarn run compile && yarn run lint",
    "test": "vscode-test",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "@types/vscode": "^1.106.1",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.1",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  }
}
